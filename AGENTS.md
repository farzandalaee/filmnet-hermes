## FilmNet Hermes Workspace

Canonical operating rules: `agents/assistant.md`.

See `~/.hermes/profiles/taskmanager/SOUL.md` for the read-map (which file to load when). Do not duplicate those pointers here.

This file exists as a marker for tools that look for `AGENTS.md` at the repo root. No behavior rules should be added here — put them in `agents/assistant.md` so they have one canonical home.
